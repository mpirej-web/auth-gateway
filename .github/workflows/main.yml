name: auth-gateway workflow
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run a command
        run: echo "Building auth-gateway project"
      - name: Simple build step
        run: |
          mkdir -p build
          touch build/auth-gateway.txt
          echo "Build complete" > build/auth-gateway.txt